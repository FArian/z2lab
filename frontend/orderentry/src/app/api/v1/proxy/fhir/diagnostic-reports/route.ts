export const dynamic = "force-dynamic";
// v1 — FHIR proxy: global results list
// Supports query: q, status, patientId, patientName, orderNumber, updatedAfter, page, pageSize
export { GET } from "@/app/api/diagnostic-reports/route";
